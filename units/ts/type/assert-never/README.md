# assert-never (TS)

**Category:** type · **Complexity:** O(1)

Exhaustive check: call with a value that should never be reached (e.g. default in switch). Throws at runtime; ensures compile-time exhaustiveness.
